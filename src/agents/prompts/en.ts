export const ENGLISH_AGENT_LANGUAGE = {
  outputLead: 'Write all newly generated human-facing prose in',
  preserve:
    'Keep user-provided issue text, code, identifiers, paths, commands, terminal output, Git content, protocol tokens, and existing conversation text exactly as supplied; do not translate or rewrite them.',
} as const;
